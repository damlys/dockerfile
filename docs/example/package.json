{
  "name": "@your/super-app",
  "version": "1.0.0",
  "files": [
    "src"
  ],
  "bin": {
    "super-app": "src/bin/super-app.js"
  },
  "devDependencies": {
    "@damlys/dockerfile": "*"
  },
  "scripts": {
    "docker:build": "dockerfile compose | docker-compose --file - build",
    "docker:publish": "dockerfile compose | docker-compose --file - push"
  },
  "dockerfile": {
    "build": {
      "args": [
        "YOUR_SECRET_TOKEN"
      ]
    }
  }
}
